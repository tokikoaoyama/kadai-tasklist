<h1>id: <%= @task.id %> の詳細ページ</h1>

<table class="table table-bordered">
    <tr>
        <th>id:</th>
        <td><%= @task.id %></td>
    </tr>
    <tr>
        <th>status</th>
        <td><%= @task.status %></td>
    </tr>
    <tr>
        <th>content</th>
        <td><%= @task.content %></td>
    </tr>
</table>

<%= link_to 'タスクを編集する', edit_task_path(@task), class: 'btn btn-light' %>
<%= link_to 'タスクを削除する', @task, method: :delete, data: { confirm: '本当に削除して宜しいですか？' }, class: 'btn btn-danger' %>